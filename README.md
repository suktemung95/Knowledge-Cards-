# Contributions

- histogram similarity function ideas were taken from code examples, but were not directly copied

# templates

- the four html files in /templates are the frontend visuals for every page in my project
- each corresponds to a certain function of my website

# models

- the KnowledgeCard model is a model that represents the cards that this project is based on
- each KnowledgeCard has four attributes: title, desc, url, and histogram (into a string)

# similarity

- this is the copied code from the example, comparing two histograms of images and finding the 'similarity' between the two

# urls

- this is where the links to each of the pages on my website are mapped to a different view, which defines the functionality of each page

# views

- the functionality of each page in my project

# config/settings

- here, I changed the allowed_hosts to allow anyone to connect to the website, rather than defining each IP address individually
